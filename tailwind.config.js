// tailwind.config.js
import { defineConfig } from 'tailwindcss';

export default defineConfig({
  theme: {
    extend: {
      fontFamily: {
        sans: ['Rubik', 'sans-serif'], // Rubik as the default sans font
      },
    },
  },
  plugins: [],
});
